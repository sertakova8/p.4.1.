min = int(input('Сколько минут Вы провели в душе? '))
if (min != int) or (min < 0)
    min = int(input('Введите целое положительно число!'))
res = (min * 6) / 0.5
print('\nВы потратили', int(res), 'бутылочек')
